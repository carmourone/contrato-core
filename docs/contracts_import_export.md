# Contract Import / Export

Contrato supports importing and exporting contracts independently of models.

Planned CLI flags:
  --import-contracts <file>
  --export-contracts <file>

Contracts are versioned and pinned to (tenant_id, model_id, model_version).
